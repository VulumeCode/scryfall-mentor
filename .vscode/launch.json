{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "firefox",
            "request": "launch",
            // "reAttach": true,
            "name": "Launch SFMentor",
            "reloadOnChange": "${workspaceFolder}/build/**/*.*",
            "addonPath": "${workspaceFolder}/build"
        }
    ]
}
